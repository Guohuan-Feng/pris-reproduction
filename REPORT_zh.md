# PRIS 复现报告

[English](REPORT.md) | 中文

日期：2026-09-14。论文：[Autonomous discovery of new structure-plausibility laws for explainable and rapid crystal diagnosis and screening](https://arxiv.org/abs/2609.01209)。代码：[AI4QC/PRIS](https://github.com/AI4QC/PRIS)，固定提交 `34e6c86c083759dc1ee594ae22238ea9b5ebd8f4`。

## 结论与完成范围

已经实际运行作者的 PRIS 分析器，重新计算公开晶体结构，独立拟合公开能量—体积数据，并保存了输入、逐样本输出、图表和复跑脚本。**这是公开数据完整部分的复现；尚不能称为论文全部实验的端到端复现。**

| 内容 | 实际执行与结果 | 能支持的结论 |
|---|---|---|
| PRIS 结构分析 | 30 个 COD 实验来源母体＋150 个固定扰动，共 180 个结构全部处理完成 | 公开 E3 子集上的功能和机制复核 |
| Fig. 3 数值例子 | 尖晶石及五种扰动重新计算，作者分析器的 10 个测试全部通过 | 关键示例和规则实现可以复算 |
| E4 体模量 | 1300 条记录中取 1297 个有效 E(V) 点，独立拟合 260/260 个候选 | 从公开 DFT 数值到体模量的推导可复算；没有新运行 DFT |
| E4 原始结构对称性 | 260 对、520 个结构重新计算；Law 7 通过数 61→113，逐结构无差异 | 公开原始坐标到对称性结论的直接复现 |
| 同一晶体的表示检查 | 7 个周期几何控制全部通过；发现 NaCl 晶胞表示会改变 Set 4 和 PSS | 原始部署接口存在表示敏感性 |
| 原文图表 | Fig. 1、3、5 重运行成功；Fig. 2 缺少必要输入 | 图表重绘与原始结构重算分别记录 |

所有规则阈值与 PSS 系数保持作者版本，没有为了匹配结果重新调参。Python 3.12.14；完整依赖版本见 `requirements-lock.txt`。

## 1. 原始晶体结构计算

数据直接取自作者仓库 `dft/E3_crosscheck/tasks/`，保留 `POSCAR.init` 原字节。每个文件的来源、COD ID、标签、操作类型和 SHA256 均记录于 [数据清单](data/original_e3/manifest.csv)。受损变体明确标为 synthetic damage，没有把上游统一的 `kind=experimental` 误当成全部都是正例。

| 规则集 | 实验结构通过 / 30 | 实验结构拒绝 / 无法判定 | 扰动检出 / 150 | 扰动通过 / 无法判定 |
|---|---:|---:|---:|---:|
| Set 1 | 30（100%） | 0 / 0 | 41（27.33%） | 101 / 8 |
| Set 1′ | 30（100%） | 0 / 0 | 58（38.67%） | 84 / 8 |
| Set 2 | 30（100%） | 0 / 0 | 86（57.33%） | 56 / 8 |
| Set 3 | 30（100%） | 0 / 0 | 105（70.00%） | 37 / 8 |
| Set 4 | 27（90.00%） | 2 / 1 | 119（79.33%） | 20 / 11 |

**“无法判定”留在总分母内，既不算通过，也不算检出。** Set 4 的 90% 并不意味着其余 10% 都被判为不合理。原始论文某些 benchmark 将缺失特征当作满足条件；本复现采用公开分析器的部署语义，不能混用两种口径。

![E3 结构复算结果](results/structure_benchmark/structure_benchmark.png)

Set 4 对五类扰动的检出分别为：单轴压缩 18/30、阳离子换位 26/30、随机位移 30/30、整体膨胀 21/30、阴阳离子换位 24/30。详见 [逐结构结果](results/structure_benchmark/structure_results.csv)、[逐规则结果](results/structure_benchmark/law_results.csv) 和 [完整结果](results/structure_benchmark/records.json)。

![各类扰动检出率](results/structure_benchmark/damage_classes.png)

### 这组数据的限制

E3 取自 **discovery split**，并按 source_id 顺序选取前 30 个符合条件的母体：有序、2–16 位点、整数价态可分配、五种扰动均可应用、换位不是等价操作，而且**全部变体的最小距离都必须 ≥0.9 Å**。它不是随机抽取的独立测试集。

因此 0.5 Å 和 0.7 Å 基线在这组数据上检出率都是 0%，这是筛选条件导致的结果。不能用这组差距宣称一般性能提升，也不能把这里的 79.33% 当成对论文 87.9% 或 91.1% 的直接复现失败/成功：它们的数据、筛选和缺失值口径不同。

两个被 Set 4 拒绝的实验来源母体为 AgAsSe2（COD 1509200）和 CrAgSe2（COD 1509271），均触发 Law 7；Cr2AgTe4（COD 1509274）因 Law 8 测量缺失而无法判定。第二个拒绝样本还缺少 Law 8，但已有 Law 7 失败，故整体仍判拒绝。这说明需要保留具体失败原因。

## 2. 独立重拟合 260 个体模量

输入为作者发布的逐任务 DFT 数值，不是直接复制最终体模量表。使用独立实现的三阶 Birch–Murnaghan 状态方程和 SciPy `least_squares`，从每个候选的体积与静态能量重新拟合，再与作者结果比较。

- 1300 条任务记录：1296 complete、1 unconverged、3 failed；按作者协议保留前两类。
- 使用 1297 个 E(V) 点，257 个候选有 5 点，3 个候选有 4 点。
- 260/260 个候选拟合成功。
- 与作者体模量的最大绝对差：**0.000232527 GPa**；中位绝对差约 **5.09×10⁻⁸ GPa**。
- 最大相对差约 **0.00009615%**。

这验证了公开能量数据到体模量的数值处理；并不验证 DFT 输入、赝势、电子收敛或机器学习势本身是否准确。输入中那条 unconverged 记录按原协议保留，未隐藏。

![体模量重新拟合及筛选阈值](results/eos/eos_reproduction.png)

### “保留 99.2%”必须带上阈值

在公开的 260 个选定候选上，重新计算得到：

| 判断阈值 | 达标数 | 保留数 | 被筛除数 |
|---|---:|---:|---:|
| 作者由 UMA 映射到 DFT 的 375.81874 GPa | 124 | 123（99.19355%） | 1 |
| 直接按原始 400 GPa 判断 DFT 值 | 2 | 1 | 1 |

两个实际 DFT 超过 400 GPa 的候选分别是：`candidate_0017`（Os，priority，约 400.38451 GPa）和 `candidate_0980`（Re2IrOs6，screened，约 418.54561 GPa）。后者是这 260 个中体模量最高的候选，却属于筛除组。这里只陈述公开样本的结果；样本经过有意选择，不能外推整个生成空间的成功率。

完整数据见 [逐候选拟合表](results/eos/per_candidate.csv)、[E(V) 点](results/eos/energy_volume_points.csv) 和 [EOS 复算说明](results/eos/README.md)。

## 3. 260 对原始结构的对称性复算

独立读取 260 个生成态 POSCAR 和对应的 260 个 DFT 弛豫后 CIF，以 `symprec=0.01 Å` 重新求解空间群和不等价位点比例。按公开补充数据导出程序的**输入晶胞口径**，结果与每一行公开 index 完全一致：

- 生成态通过 Law 7：**61/260**。
- 弛豫后通过 Law 7：**113/260**。
- 新增通过 **52**，失去通过 **0**。
- 逐结构空间群编号、位点比例和判定：**0 行不一致**。

这部分是真正从原始坐标重新计算，使用已发布的弛豫后结构，没有重新执行产生这些结构的 DFT 计算。作者选过 261 个输入，但其中 `candidate_0248` 没有公开弛豫后 CIF；本复算采用与公开补充数据一致的固定 260 对。

统一到标准原胞后另算一次作为敏感性分析，结果变为 **66→110**，新增 44。9 个生成态和 3 个弛豫后判定与输入晶胞口径不同。这一步可能在容差范围内理想化坐标，故不能把所有变化仅归因于纯粹的晶胞重复。数据见 [对称性复算说明](results/symmetry/README.md) 和 [逐结构结果](results/symmetry/recomputed_symmetry.csv)。

## 4. 发现的表示敏感性

构造同一个理想 NaCl 无限周期晶体，仅改变输入晶胞表示，得到：

| 表示 | 位点数 | Law 7 数值 | Set 4 |
|---|---:|---:|---|
| 原胞 | 2 | 1.000 | 不合理 |
| 常规晶胞 | 8 | 0.250 | 合理 |
| 原胞 2×2×2 超胞 | 16 | 0.125 | 合理 |

其余七条规则的判定一致。原因是 Law 7 直接使用“不等价位点数 / 输入位点数”，部署入口没有统一晶胞。PSS 也会变化，其位点复杂度和部分多面体图特征都对输入表示敏感。**复现没有修补原代码或挑选能通过的晶胞。**

![同一 NaCl 晶体的不同判定](results/core_validation/cell_representation.png)

这提出了一个具体研究问题：先区分论文不同实验的晶胞规范，再测试一个统一表示、正确处理周期邻接的诊断工具。仅统一成原胞并不自动保证规则科学有效，因为 NaCl 原胞仍会触发当前 Law 7，需要重新评估该经验规则的适用范围。

检验记录见 [核心验证说明](results/core_validation/core_validation.md)。作者的 10 个 analyzer 测试全部通过；另外 6 个历史 regression 测试中 5 个通过，1 个因仓库缺少 `src/build_bonds.py` 而失败，错误日志保留。不是把整个仓库的测试都说成通过。

## 5. 图表与未完成部分

- Fig. 1：从作者提交的汇总数据重绘；没有重跑 agent 搜索。
- Fig. 3：说明性 MgAl2O4 的结构与五种扰动重新计算。母体 ρ=0.986487，压缩后 ρ=0.756613，膨胀后 ρ=1.282433；与正文舍入值一致。阳离子交换 S2 在这个示例中未触发规则，也保留了这一结果。其他面板使用公开汇总/DFT 数值。
- Fig. 5：重绘公开汇总和 DFT 输出。文件保留作者旧名 `fig6_deployment`。
- Fig. 2：尝试后确认缺 `outputs/20260815_threshold_transfer/transfer.json`，无法完整重绘。
- Fig. 4：需要未发布的 PU 分数分片和完整逆向设计数据，无法完整重绘。

成功图表、日志与每张图的计算层级见 [图表复跑清单](results/author_figures/README.md)。

完整 5,297/3,612 留出集、440 个母体的部署基准、几 GB 特征表、部分结构 blob 和完整 1,081 个逆向设计候选没有随仓库提供。不能用 E3 子集或现有聚合曲线代替这些原始输入。原始 200 万次候选搜索和新的 VASP 计算也没有执行。

另一个需要区分的原文口径：[仓库 STATUS.json](https://github.com/AI4QC/PRIS/blob/34e6c86c083759dc1ee594ae22238ea9b5ebd8f4/outputs/20260823_fig45_merged_nature_inverse_f_v1/STATUS.json) 报告 Set 4 剔除 728/1081（67.345%），但保留 UMA≥400 的 45/140；PSS 某阈值剔除 61/1081（5.643%），保留 140/140。**这是作者发布的聚合计数，尚未独立复算；不能把两个策略的数字拼成一个成功率。**

## 6. 后续研究

可以进一步评估：明确晶胞规范并统一周期图的邻接处理，能否改善 PRIS 诊断结果的可靠性。首先应厘清上游不同分析程序采用的输入晶胞和标准原胞约定，取得缺失的原始基准输入，并在报告分母中保留全部无法判定样本。

任何修改后的晶胞处理方式或相关描述符，都应先在开发集上设计，并在必要时重新校准，再用于固定留出集。比较应报告实验结构通过率、扰动检出率、可判定覆盖率，以及等价晶胞表示下的结果敏感性。应以未修改的分析器作为基线，并将绝对性质目标与 UMA—DFT 映射阈值分开。这是基于本次复现发现的具体扩展方向，而不是把经筛选的 E3 诊断子集当作一般性能的独立证据。

复跑入口与依赖安装见 [README](README.md)。
